Operadores Relacionais:

5 > 2 > true
7 < 4 > false
8 >= 8 > true
9 <= 7 > false
5 == 5 > true
4 != 4 > false
4 === '4' > false
5 !== '5' > true

Identidade:
5 == 5 > true
5 == '5' > true //a grandeza é igual
5 === '5' > false //com 3 "=",  checa se é o mesmo valor
e o mesmo TIPO.
5 === 5 > true //5 é identico ao 5? SIm.

Operadores Lógicos:

! = negação
&& = conjunção
|| = disjunção

Exemplo:
negação = Eu quero qualquer caneta, menos azul.
conjunção = Eu quero uma caneta azul E uma vermelha.
disjunção = Eu quero uma caneta azul OU uma vermelha.

Explicação dos Operadores Lógicos:

Negação:
! = Tratado como operador unário, ou seja, apenas um operando.
Depois da exclamação, eu boto true ou false, ou eu boto uma
expressão que vai dar true ou false.

! >True > False //Uma coisa que não é verdadeira é o que?
! >False > True //Uma coisa que não é false é o que?

Conjunção:
&& = Operador binário, dois valores lógicos, um de cada lado.
True && True -> True
True && False -> False		// APENAS ME SATISFAZ SE
False && True -> False		// AS DUAS FOREM VERDADEIRAS.
False && False -> False

Disjunção:
|| = Também é um operador binário = dois valores lógicos de cada lado.
Dependendo da posição deles e também do resultado lógico, eu também
tenho o valor lógico de resultado.
Na disjunção, basta que apenas um deles seja verdadeiro, para que
o resultado seja verdadeiro.

True || True -> True
True || False -> True		// QUALQUER RESUTLADO VERDADEIRO
False || True -> True		// ME SATISFAZ.
False || False -> False

* Primeiro é feito o operador aritmético, depois eu vou fazer
todos os operadores relacionais e depois eu vou fazer os
operadores lógicos.

a = 5
b = 8
a <= b || b / 2 == 2 -> True

* Primeiro o NÃO, depois o E, depois o OU.

//Exemplos:
idade >= 15 && idade <= 17 // a idade está entre 15 e 17?
estado == 'RJ' || estado == 'SP' // o estado é RJ ou SP? 
salário > 1500 && sexo != 'M' // o salário é acima de 1500 e
não é um homem?

Precedência ~ ATUALIZADA ~:
() ** / (operadores aritméticos)
> < >= (operadores relacionais)
!
&&
||

Operador Ternário:
? :
Ele se chama ternário porque ele tem três partes.
Esses blocos são blocos de teste, bloco verdadeiro e o
bloco falso.
teste ? true : false

Exemplo:
média >= 7.0 ? "Aprovado" : "Reprovado"


